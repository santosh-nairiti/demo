{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "minify-css": "gulp-minify-css",
    "uglify-js": "gulp-uglify-js",
    "build": "npm run minify-css && npm run uglify-js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp-minify-css": "^1.0.0",
    "gulp-uglify-js": "^1.0.0"
  }
}
